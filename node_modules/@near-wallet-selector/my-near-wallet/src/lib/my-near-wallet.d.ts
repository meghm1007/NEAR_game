import type { WalletModuleFactory, InjectedWallet } from "@near-wallet-selector/core";
export interface MyNearWalletParams {
    walletUrl?: string;
    iconUrl?: string;
    deprecated?: boolean;
    successUrl?: string;
    failureUrl?: string;
}
export declare function setupMyNearWallet({ walletUrl, iconUrl, deprecated, }?: MyNearWalletParams): WalletModuleFactory<InjectedWallet>;
